scheme SUBTYPE_AXIOM =
    class
      type
        NonZero = {| x : A :- x ~= 0 |},
        A = Int
      value
        inverse : NonZero -> Int
        inverse(x) is 1 / x
      axiom
        [inverseProperty] (all x : NonZero :- inverse(x) * x = 1)
    end
  